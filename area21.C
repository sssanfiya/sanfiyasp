#include<stdio.h>
int main()
{
double l,b,a;
printf("\n enter the length of the farm:");
scanf("%lf",&l);
printf("\n enter the breadth of the farm:");
scanf("%lf",&b);
a=l*b;
printf("the  area is:%lf",a);
return 0;
}
